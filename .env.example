# 对外暴露的端口（nginx 容器映射到宿主机）
HTTP_PORT=8080

# 服务器域名；没有域名就保持 _
SERVER_NAME=_

# 可选：manage.sh upgrade 使用的分支（默认使用当前分支）
# LEDGER_BRANCH=main

# 可选：容器以哪个 uid:gid 运行（默认宿主机当前用户，manage.sh 会自动导出）
# APP_UID=1000
# APP_GID=1000
